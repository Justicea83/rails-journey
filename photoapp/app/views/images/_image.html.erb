<div id="<%= dom_id image %>">
  <p>
    <strong>Name:</strong>
    <%= image.name %>
  </p>

  <p>
    <strong>Picture:</strong>
    <%= image_tag(image.picture.url, size: '250x150') if image.picture? %>
  </p>

</div>
